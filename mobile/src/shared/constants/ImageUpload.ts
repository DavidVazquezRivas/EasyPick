export const ImageUploadLimits = {
  SERVER_MAX_FILE_SIZE_BYTES: 15 * 1024 * 1024,
  CLIENT_MAX_FILE_SIZE_BYTES: 14 * 1024 * 1024,
  MAX_DIMENSION_PX: 2560,
  INITIAL_COMPRESSION_QUALITY: 0.78,
  SECOND_PASS_COMPRESSION_QUALITY: 0.62,
} as const
